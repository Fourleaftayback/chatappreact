import React from "react";
import { Button } from "reactstrap";

const GroupChatButton = () => {
  return (
    <React.Fragment>
      <Button>IconButton with Modal Here</Button>
    </React.Fragment>
  );
};

export default GroupChatButton;
